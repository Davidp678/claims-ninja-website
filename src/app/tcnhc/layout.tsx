import { helpPortalMetadata } from "@/lib/help-center/metadata";

export const metadata = helpPortalMetadata("internal");

export default function TcnhcLayout({ children }: { children: React.ReactNode }) {
  return children;
}

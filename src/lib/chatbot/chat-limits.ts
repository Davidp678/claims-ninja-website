/** Max characters per chat message (client + server). */
export const MAX_CHAT_MESSAGE_CHARS = 2000;

/** Max history items accepted in POST /api/chat body. */
export const MAX_CHAT_HISTORY_ITEMS = 10;

/** Max messages sent to OpenAI as conversation context. */
export const MAX_CHAT_CONTEXT_MESSAGES = 8;
